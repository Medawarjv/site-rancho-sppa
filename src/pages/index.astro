---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import site from "../data/site.json";
import heroImg from "../assets/hero.jpg";
import piscinaImg from "../assets/bloco-piscina.jpg";
import quiosqueImg from "../assets/bloco-quiosque.jpg";
import acomodacoesImg from "../assets/bloco-acomodacoes.jpg";
import esportesImg from "../assets/bloco-esportes.jpg";
import naturezaImg from "../assets/bloco-natureza.jpg";
import represaImg from "../assets/bloco-represa.jpg";
import Galeria from "../components/Galeria.astro";
import Calendario from "../components/Calendario.astro";
import Regras from "../components/Regras.astro";
import ComoChegar from "../components/ComoChegar.astro";
import Avaliacoes from "../components/Avaliacoes.astro";
import Comodidades from "../components/Comodidades.astro";
import BarraFatos from "../components/BarraFatos.astro";
import ParaQuem from "../components/ParaQuem.astro";
import TituloSecao from "../components/TituloSecao.astro";
import Icon from "../components/Icon.astro";
import avaliacoes from "../data/avaliacoes.json";

// WhatsApp com mensagem contextual por seção (a mensagem NÃO é enviada
// sozinha — o usuário confirma; só já dá o contexto pra Lorena).
const wa = (msg: string) =>
  `https://wa.me/${site.whatsapp}?text=${encodeURIComponent(msg)}`;
const waHero = wa("Olá! Vi o site do Rancho SPPA e quero consultar a disponibilidade.");
const waFinal = wa(
  "Olá! Quero marcar uma data no Rancho SPPA. Pode me passar a disponibilidade e os valores?",
);

// Dados estruturados para o Google (rich results).
const schema = {
  "@context": "https://schema.org",
  "@type": "LodgingBusiness",
  "@id": "https://sppa.com.br/#lodging",
  name: site.nome,
  alternateName: site.nomeCompleto,
  description:
    "Rancho para aluguel por temporada às margens da Represa de Miranda, em Indianópolis-MG, a cerca de 1 hora de Uberlândia. Piscina aquecida, 10 suítes para até 30 pessoas, quiosque com churrasqueira, esportes e natureza.",
  url: "https://sppa.com.br",
  image: ["https://sppa.com.br/og.jpg"],
  telephone: `+${site.whatsapp}`,
  hasMap: site.mapsLink,
  numberOfRooms: 10,
  petsAllowed: true,
  paymentAccepted: "PIX, Dinheiro",
  areaServed: ["Indianópolis", "Uberlândia", "Triângulo Mineiro"],
  address: {
    "@type": "PostalAddress",
    addressLocality: "Indianópolis",
    addressRegion: "MG",
    postalCode: "38490-000",
    addressCountry: "BR",
  },
  geo: {
    "@type": "GeoCoordinates",
    latitude: Number(site.mapsLat),
    longitude: Number(site.mapsLng),
  },
  aggregateRating: {
    "@type": "AggregateRating",
    ratingValue: "4.7",
    reviewCount: "68",
    bestRating: "5",
  },
  amenityFeature: [
    "Piscina aquecida por energia solar",
    "Quiosque com churrasqueira e forno de pizza",
    "10 suítes com ar-condicionado",
    "Wi-Fi",
    "Acesso à Represa de Miranda com píer",
    "Quadra de beach tennis",
    "Trilhas e cachoeiras",
    "Pet friendly",
  ].map((nome) => ({
    "@type": "LocationFeatureSpecification",
    name: nome,
    value: true,
  })),
  sameAs: [site.mapsLink, site.instagram, site.facebook].filter(Boolean),
};

// Os 6 blocos do rancho (escopo §4.2). Texto adaptado do material aprovado.
const blocos = [
  {
    img: piscinaImg,
    alt: "Piscina azul do Rancho SPPA entre coqueiros, em dia de sol",
    titulo: "Piscina aquecida",
    texto:
      "Piscina aquecida por energia solar, para curtir a água com conforto em qualquer época do ano.",
  },
  {
    img: quiosqueImg,
    alt: "Quiosque com churrasqueira e forno de pizza",
    titulo: "Quiosque & churrasqueira",
    texto:
      "Quiosque com churrasqueira, fogão a lenha e forno de pizza, o ponto de encontro para reunir a família toda.",
  },
  {
    img: acomodacoesImg,
    alt: "Chalés de acomodação do Rancho SPPA entre os coqueiros",
    titulo: "Acomodações",
    texto:
      "10 suítes com ar-condicionado e cama de casal. As maiores têm camas de solteiro ou beliches, acomodando até 30 pessoas. Cozinha equipada, Wi-Fi, gás e água potável.",
  },
  {
    img: esportesImg,
    alt: "Quadra de areia e playground do rancho em dia de sol",
    titulo: "Esportes & lazer",
    texto:
      "Campo de futebol, quadra de areia para beach tennis, peteca ou vôlei, além de sinuca, ping-pong e playground.",
  },
  {
    img: naturezaImg,
    alt: "Riacho e cachoeira em meio à mata do rancho",
    titulo: "Natureza",
    texto:
      "70.000 m² de área verde com trilhas, pomar, riacho e cachoeiras para explorar sem pressa.",
  },
  {
    img: represaImg,
    alt: "Píer do rancho às margens da Represa de Miranda",
    titulo: "Represa de Miranda",
    texto:
      "450 metros de margem, com píer, tablado de pesca e redutor para descer lancha ou jet ski de até 1 tonelada.",
  },
];

---

<Layout>
  <script type="application/ld+json" set:html={JSON.stringify(schema)} />

  <!-- HERO (full-bleed) -->
  <header
    id="hero"
    class="relative flex min-h-[80vh] items-end overflow-hidden sm:min-h-[90vh]"
  >
    <Image
      src={heroImg}
      alt="Vista da área de lazer do Rancho SPPA em dia de sol: quadra de areia, piscina, playground, quiosque e coqueiros"
      class="animate-kenburns absolute inset-0 h-full w-full object-cover object-[center_78%]"
      widths={[768, 1280, 1600]}
      sizes="100vw"
      loading="eager"
      fetchpriority="high"
    />
    <!-- scrim de baixo pra cima (mais escuro onde fica o texto) -->
    <div class="absolute inset-0 bg-gradient-to-t from-black/75 via-black/30 to-black/5"></div>

    <!-- Marca (wordmark tipográfico, mesma estrutura do logo oficial) -->
    <a
      href="/"
      aria-label="Rancho Pica-Pau MG, desde 2001. Voltar ao início"
      class="absolute inset-x-0 top-[calc(1.25rem+env(safe-area-inset-top))] z-20 mx-auto flex w-fit flex-col items-center drop-shadow-[0_1px_6px_rgba(0,0,0,0.55)] sm:inset-x-auto sm:left-8 sm:top-[calc(1.75rem+env(safe-area-inset-top))] sm:mx-0 sm:items-start"
    >
      <span class="font-marca text-[1.75rem] uppercase leading-none tracking-wide text-white sm:text-4xl lg:text-5xl">
        Rancho <span class="text-dourado">Pica-Pau</span> MG
      </span>
      <span class="mt-1.5 flex items-center gap-2 text-[9px] font-medium uppercase tracking-[0.35em] text-white/85 sm:text-[11px] lg:text-xs">
        <span class="h-px flex-1 bg-white/60"></span>
        Desde 2001
        <span class="h-px flex-1 bg-white/60"></span>
      </span>
    </a>

    <!-- pt reserva espaço pra marca: o título nunca encosta nela no mobile -->
    <div class="relative z-10 mx-auto w-full max-w-6xl px-6 pb-14 pt-24 sm:pb-20 sm:pt-32">
      <div class="max-w-2xl text-white">
        <h1 class="font-titulo text-4xl font-semibold leading-[1.05] drop-shadow-md sm:text-6xl xl:text-7xl">
          Seu refúgio na Represa de Miranda
        </h1>
        <p class="mt-5 max-w-xl text-lg text-white/90 drop-shadow sm:text-xl">
          Rancho para aluguel por temporada em Indianópolis-MG, a cerca de
          1&nbsp;hora de Uberlândia. Natureza, água e sossego para a família toda.
        </p>
        <div class="mt-8 flex flex-wrap items-center gap-3">
          <a
            href={waHero}
            target="_blank"
            rel="noopener"
            class="inline-block rounded-full bg-zap px-8 py-4 text-base font-semibold text-white shadow-xl transition hover:-translate-y-0.5 hover:bg-zap-escuro"
          >
            Consultar disponibilidade
          </a>
          <a
            href="#rancho"
            class="inline-block rounded-full border border-white/50 px-7 py-4 text-base font-medium text-white backdrop-blur-sm transition hover:bg-white/10"
          >
            Conhecer o rancho
          </a>
        </div>

        <!-- Prova social -->
        <p class="mt-7 flex flex-wrap items-center gap-x-2 gap-y-1 text-sm text-white/85">
          <span class="flex text-dourado">
            {[0, 1, 2, 3, 4].map(() => <Icon name="star" class="h-4 w-4" />)}
          </span>
          <span>
            <strong>{avaliacoes.resumo.nota}</strong> · {avaliacoes.resumo.total} avaliações no Google
          </span>
        </p>

        <!-- Localização (abaixo da prova social, longe da marca no mobile) -->
        <p class="mt-5 flex items-center gap-3 text-xs font-medium uppercase tracking-[0.2em] text-white/85">
          <span class="h-px w-8 bg-dourado"></span>
          Indianópolis · MG · Represa de Miranda
        </p>
      </div>
    </div>
  </header>

  <!-- BARRA DE FATOS -->
  <BarraFatos />

  <!-- O RANCHO — 6 blocos -->
  <section id="rancho" class="bg-areia py-14 sm:py-20">
    <div class="mx-auto max-w-6xl px-6">
    <TituloSecao
      kicker="A estrutura"
      titulo="Tudo que o rancho oferece"
      sub="7 hectares de estrutura completa para descansar, se divertir e curtir a represa com quem você ama."
    />

    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {
        blocos.map((b) => (
          <article class="group overflow-hidden rounded-2xl bg-white ring-1 ring-terra/10 shadow-sm transition duration-300 hover:-translate-y-1 hover:shadow-[0_18px_40px_-18px_rgba(30,63,39,0.35)]">
            <div class="overflow-hidden">
              <Image
                src={b.img}
                alt={b.alt}
                class="h-56 w-full object-cover transition duration-500 group-hover:scale-105"
                widths={[400, 640, 800]}
                sizes="(min-width: 1024px) 352px, (min-width: 640px) 50vw, 100vw"
                loading="lazy"
              />
            </div>
            <div class="p-6">
              <h3 class="font-titulo text-xl font-semibold text-mata-escura">{b.titulo}</h3>
              <p class="mt-2 text-sm leading-relaxed text-tinta/70">{b.texto}</p>
            </div>
          </article>
        ))
      }
    </div>
    </div>
  </section>

  <!-- GALERIA -->
  <Galeria />

  <!-- PARA QUEM É (3 perfis com CTA contextual) -->
  <ParaQuem />

  <!-- DISPONIBILIDADE (calendário via iCal) -->
  <Calendario />

  <!-- REGRAS PRINCIPAIS -->
  <Regras />

  <!-- COMO CHEGAR (mapa) -->
  <ComoChegar />

  <!-- COMODIDADES E PARCEIROS -->
  <Comodidades />

  <!-- PROVA SOCIAL -->
  <Avaliacoes />

  <!-- CTA FINAL -->
  <section id="contato" class="bg-mata px-6 py-20 text-center text-areia sm:py-24">
    <h2 class="font-titulo text-3xl font-bold sm:text-4xl">
      Bora marcar a sua data?
    </h2>
    <p class="mx-auto mt-4 max-w-xl text-areia/85">
      A Lorena responde rapidinho no WhatsApp: tira dúvidas, confirma
      disponibilidade e passa os valores. Sem compromisso.
    </p>
    <a
      href={waFinal}
      target="_blank"
      rel="noopener"
      class="mt-8 inline-block rounded-full bg-zap px-8 py-4 text-lg font-semibold text-white shadow-xl transition hover:scale-105 hover:bg-zap-escuro"
    >
      Consultar disponibilidade
    </a>

    <!-- Prova social junto do botão final -->
    <p class="mt-6 flex flex-wrap items-center justify-center gap-x-2 gap-y-1 text-sm text-areia/80">
      <span class="flex text-dourado" aria-hidden="true">
        {[0, 1, 2, 3, 4].map(() => <Icon name="star" class="h-4 w-4" />)}
      </span>
      <span>
        <strong class="text-areia">{avaliacoes.resumo.nota}</strong> · {avaliacoes.resumo.total} avaliações no Google
      </span>
    </p>
  </section>

</Layout>
